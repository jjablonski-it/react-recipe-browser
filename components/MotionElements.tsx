import { Card, CardHeader, Grid, IconButton } from "@material-ui/core";
import { motion } from "framer-motion";

export const MotionGrid = motion.custom(Grid);
export const MotionCard = motion.custom(Card);
export const MotionCardHeader = motion.custom(CardHeader);
export const MotionIconButton = motion.custom(IconButton);
